# dependencies & build output
node_modules/
dist/
coverage/

# local runtime artifacts (never commit personal library data)
state.json
config.json
*.log
changes.log
agent.out.log
agent.err.log
.DS_Store

# personal library export (never commit)
Library.xml
*.musiclibrary
